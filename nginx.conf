worker_processes auto;
events {
    worker_connections 1024;
}

http {
    server {
        listen 8080;
        server_name localhost;
        client_max_body_size 50M;

        location ~/backend$ {
            proxy_pass http://generate:8000;
            proxy_set_header Host $host;
        }

    }
}